---
description: Manual override for frontend-polisher.
agent: frontend-polisher
subtask: true
---

Handle this UI polish task.

Use the `frontend-polisher` prompt and shared rules. Treat `$ARGUMENTS` as the full polish assignment. Return the required task protocol.

$ARGUMENTS
