---
description: Manual override for security-reviewer.
agent: security-reviewer
subtask: true
---

Review the assigned code for security issues and return findings.

Use the `security-reviewer` prompt and shared rules. Treat `$ARGUMENTS` as the full review assignment. Return the required review protocol.

$ARGUMENTS
