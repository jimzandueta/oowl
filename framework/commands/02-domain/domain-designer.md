---
description: Manual override for designer.
agent: designer
subtask: true
---

Create or update the UI spec for this feature.

Use the `designer` prompt and shared rules. Treat `$ARGUMENTS` as UI context and constraints. Return the required phase protocol.

$ARGUMENTS
