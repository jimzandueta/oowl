---
description: Manual override for code-reviewer.
agent: code-reviewer
subtask: true
---

Review the assigned code and return findings.

Use the `code-reviewer` prompt and shared rules. Treat `$ARGUMENTS` as the full review assignment. Return the required review protocol.

$ARGUMENTS
