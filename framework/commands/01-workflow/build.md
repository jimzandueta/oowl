---
description: Route approved implementation work through dispatcher.
agent: dispatcher
subtask: false
---

Route the approved implementation to the build phase.

Use the `dispatcher` prompt, workflow rules, and shared protocol files. Treat `$ARGUMENTS` as build context and constraints. Return the required workflow response.

$ARGUMENTS
