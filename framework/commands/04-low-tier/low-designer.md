---
description: Manual override for low-designer.
agent: low-designer
subtask: true
---

Handle this bounded non-sensitive design task.

Use the `low-designer` prompt and shared rules. Treat `$ARGUMENTS` as the full assignment. Return the required task protocol or escalation protocol.

$ARGUMENTS
