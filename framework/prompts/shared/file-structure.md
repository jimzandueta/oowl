# File Structure

Customize this file with your project's file and directory conventions.

Examples of what to define:
- Directory layout (where do components, services, tests, utils live)
- File naming patterns (kebab-case, PascalCase, snake_case)
- Test file placement (co-located vs mirror tree)
- Configuration file locations
- Where new features should be created
- Asset and static file organization

Keep this file version-controlled. It is loaded into every implementation agent's context.
